[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"


[project]
name = "mrms-usgs-events"
version = "0.1.3"
description = "Time-series peak extraction, basin delineation, state-level MRMS data download, and Zarr conversion."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }

authors = [
  { name = "Gonzalo Forero", email = "gonzaloforero57@gmail.com" },
  { name = "Jacob Johnson", email = "jjohnson@aquaveo.com" }
]

keywords = [
  "usgs",
  "mrms",
  "hydrology",
  "geoscience",
  "zarr",
  "data-pipeline"
]

classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.10",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "License :: OSI Approved :: MIT License",
  "Operating System :: OS Independent",
]


dependencies = [
  "typer>=0.9",
  "pandas",
  "xarray",
  "zarr",
  "requests",
  "pyarrow",
  "Hydro-Event-Detector",
  "geopandas",
  "orjson",
]


[project.urls]
Repository = "https://github.com/GonzaloAlbertoForeroBuitrago/mrms_usgs_events"


[project.scripts]
mrms-usgs = "mrms_usgs_events.cli:app"


[tool.setuptools.packages.find]
where = ["."]
